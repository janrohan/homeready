# Frontend is a Node.js (Vite + React) app. Use `npm install` or `pnpm` to install JS dependencies.
# This file is intentionally empty for Python - no frontend Python deps.
# For reproducibility, refer to `frontend/package.json` for exact JS deps.
